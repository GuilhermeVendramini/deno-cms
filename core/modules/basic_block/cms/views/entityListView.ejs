<%- await include(`${Deno.cwd()}${Deno.env.get('THEME')}templates/layout/header.ejs`, { title: entity.name, currentUser: currentUser }) %>
<div class="container">
  <h1 class="text-center mb-3"><%= entity.name %></h1>
  <fieldset class="border p-2">
    <legend  class="w-auto">Manager</legend>
    <div class="d-flex mb-3">
      <div class="mr-2">
        <a href='/admin/<%= entity.bundle %>/<%= entity.type %>/add' class="btn btn-outline-secondary">Add new</a>
      </div>
    </div>
  </fieldset>
  <div class="mt-3">
    <% if (block) { %>
    <table class="table">
      <thead>
        <tr>
          <th scope="col">ID</th>
          <th scope="col">Title</th>
          <th scope="col">Author</th>
          <th scope="col">Created</th>
          <th scope="col">Updated</th>
          <th scope="col">Published</th>
          <th scope="col">Operations</th>
        </tr>
      </thead>
      <tbody>
        <% for(var i=0; i < block.length; i++) { %>
        <tr>
          <th scope="row"><%= block[i]._id.$oid %></th>
          <td><a href='/<%= entity.bundle %>/<%= block[i].type %>/<%= block[i]._id.$oid %>'><%= block[i].data.title %></a></td>
          <td><%= block[i].author.name %></td>
          <td><%= block[i].created %></td>
          <td><%= block[i].updated %></td>
          <td><%= block[i].published %></td>
          <td>
            <% if (currentUser._id.$oid == block[i].author._id.$oid || currentUser.roles.includes(0)) { %>
              <a href='/admin/<%= entity.bundle %>/<%= block[i].type %>/edit/<%= block[i]._id.$oid %>'>edit</a> | <a href='/admin/<%= entity.bundle %>/<%= block[i].type %>/delete/<%= block[i]._id.$oid %>'>delete</a>
            <% } %>
          </td>
        </tr>
        <% } %>
      </tbody>
    </table>
    <% } %>
  </div>
</div>
<%- await include(`${Deno.cwd()}${Deno.env.get('THEME')}templates/layout/footer.ejs`) %>