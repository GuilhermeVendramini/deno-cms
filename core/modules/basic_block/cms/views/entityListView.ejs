<%- await include(`${Deno.cwd()}${Deno.env.get('THEME')}templates/layout/header.ejs`, { title: page.entity.name, currentUser: currentUser }) %>
<div class="container">
  <h1 class="text-center mb-3"><%= page.entity.name %></h1>
  <fieldset class="border p-2">
    <legend  class="w-auto">Manager</legend>
    <div class="d-flex mb-3">
      <div class="mr-2">
        <a href='/admin/<%= page.entity.bundle %>/<%= page.entity.type %>/add' class="btn btn-outline-secondary">Add new</a>
      </div>
    </div>
  </fieldset>
  <div class="mt-3">
    <% if (page.block) { %>
    <table class="table">
      <thead>
        <tr>
          <th scope="col">ID</th>
          <th scope="col">Title</th>
          <th scope="col">Author</th>
          <th scope="col">Created</th>
          <th scope="col">Updated</th>
          <th scope="col">Published</th>
          <th scope="col">Operations</th>
        </tr>
      </thead>
      <tbody>
        <% for(var i=0; i < page.block.length; i++) { %>
        <tr>
          <th scope="row"><%= page.block[i]._id.$oid %></th>
          <td><a href='<%= page.block[i].path %>'><%= page.block[i].data.title %></a></td>
          <td><%= page.block[i].author.name %></td>
          <td><%= page.block[i].created %></td>
          <td><%= page.block[i].updated %></td>
          <td><%= page.block[i].published %></td>
          <td>
            <% if (currentUser._id.$oid == page.block[i].author._id.$oid || currentUser.roles.includes(0)) { %>
              <a href='/admin/<%= page.entity.bundle %>/<%= page.block[i].type %>/edit/<%= page.block[i]._id.$oid %>'>edit</a> | <a href='/admin/<%= page.entity.bundle %>/<%= page.block[i].type %>/delete/<%= page.block[i]._id.$oid %>'>delete</a>
            <% } %>
          </td>
        </tr>
        <% } %>
      </tbody>
    </table>
    <% } %>
  </div>
</div>
<%- await include(`${Deno.cwd()}${Deno.env.get('THEME')}templates/layout/footer.ejs`) %>