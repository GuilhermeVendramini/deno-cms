<%- await include(`${Deno.cwd()}${Deno.env.get('THEME')}templates/layout/header.ejs`, { title: 'Confirm delete' }) %>
<% if (currentUser) { %>
  <%- await include(`${Deno.cwd()}/core/modules/admin/views/adminMenu.ejs`, { currentUser: currentUser}) %>
<% } %>
<div class="container h-100 pt-4">
  <h1 class="text-center mb-3">Confirm delete <%= term.data.title %></h1>
  <form action='/admin/taxonomy/<%= term.type.replace("_", "-") %>/delete' method=post>
    <input type="hidden" id="id" name="id" value="<%= term._id.$oid %>">
    <div class="d-flex justify-content-center">
      <div class="mr-2">
        <a href="#" onclick="window.history.back();" class="btn btn-outline-secondary">Cancel</a>
      </div>
      <div class="mr-2">
        <button type="submit" class="btn btn-danger">Confirm</button>
      </div>
    </div>
  </form>
</div>
<%- await include(`${Deno.cwd()}${Deno.env.get('THEME')}templates/layout/footer.ejs`) %>
