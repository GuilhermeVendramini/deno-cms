let type: string = "main_menu";
let bundle: string = "menu_item";
let name: string = "Main menu";
let fields: string[] = [
  "target",
  "weight",
];
let references: string[] = [];
let canBeReferenced: boolean = false;

export default {
  type,
  bundle,
  name,
  fields,
  references,
  canBeReferenced,
};
